import styled from 'styled-components';

export const Box = styled.div`
  display: flex;
  align-items:center;
  span, div, button{
      margin: 10px;
  }
`;

export const Container = styled.div`
  max-width: 800px;
  margin: 0 auto;
`;
